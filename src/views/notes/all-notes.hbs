<div class="row">

  {{#each notes}}
  <div class="col-md-3">
    <div class="card">
      <div class="card-body">
        {{!-- No hace falta especificar que estamos dentro de "notes" debido a que estamos edntro del "each", por lo que entiende que estamos recorriendo a "notes"--}}

        <h4 class="card-title d-flex justify-content-between align-items-center">
          {{title}}

          {{!--Dicho icono tendra como enlace a otra pagina/vista que sera para editar la nota --}}
          <a href="/notes/edit/{{_id}}">
            <i class="fa fa-pencil"></i>
          </a>

        </h4>

        <p>
          {{description}}
        </p>

        {{!-- Aqui hay un problema: Los formularios solamente pueden enviar peticiones "post y get"  pero para ello, utilizaremos un modulo llamado "method-override" que nos permitira tambien enviar peticiones DELETE en el formulario. Por lo que lo requeriremos en el archivo "server.js" y luego lo ejecutaremos--}}
        <form action="/notes/delete/{{_id}}?_method=DELETE" method="POST">
          <button type="submit" class="btn btn-danger btn-block btn-sm">Eliminar</button>
        </form>

      </div>
    </div>
  </div>
  {{/each}}

</div>